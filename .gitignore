# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/

# Virtual environment
.venv/
venv/

# Environment
.env
.env.local

# Training artifacts (regenerated per-run, keep examples)
models/adapters/
models/gguf/

# Per-run datasets / history / DBs (generated locally, not source).
# Already-tracked example data under data/ is unaffected (gitignore does not
# untrack), so the built-in example datasets stay in the repo.
data/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# ML caches (huge, download once)
.huggingface/
.cache/huggingface/

# Logs
*.log
/tmp/
logs/

# Mise
.mise.toml

# Demo video output
*.mp4
*.webm
