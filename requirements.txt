# Fine-Tuning Platform dependencies — cross-platform (macOS + Linux).
#
# Apple Silicon uses the MLX backend; Linux (and any non-Mac) uses the
# HuggingFace/PyTorch CPU backend. Environment markers ensure each platform's
# native deps never get pulled in (or block install) on the other.
#
# Linux/CPU tip: install the lighter CPU PyTorch wheel to avoid the large CUDA
# download:  pip install torch --index-url https://download.pytorch.org/whl/cpu
# (run that before this file; the pin below stays compatible.)

# ── Cross-platform core (web UI, data layer, HF backend) ────────────
accelerate==1.14.0
datasets==5.0.0
duckdb==1.5.4
fastapi==0.136.3
huggingface_hub==1.19.0
peft==0.19.1
pydantic==2.13.4
pyyaml==6.0.3
safetensors
torch==2.12.0
transformers==5.12.0
trl==1.6.0
uvicorn==0.49.0

# ── Apple Silicon only (MLX training/inference backend) ─────────────
mlx==0.31.2 ; sys_platform == 'darwin'
mlx-lm==0.31.3 ; sys_platform == 'darwin'
mlx-metal==0.31.2 ; sys_platform == 'darwin'

# ── Optional RAG vector compression (Apple-oriented; unused by core) ─
turbovec==0.8.0 ; sys_platform == 'darwin'
